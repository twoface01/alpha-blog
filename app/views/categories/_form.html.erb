<%= render 'shared/form_errors', obj: @category %>
<div class="bg-light shadow rounded p-4 mb-4">
  <%= form_with(model: @category, local: true) do |f| %>
  <div class="form-group row">
    <div class="col-3">
      <%= f.label :name, "Category Name", class: "col-form-label font-weight-bold text-left" %>
    </div>
    <div class="col-9">
      <%= f.text_field :name, class: "form-control w-100", placeholder: "Enter a category name" %>
    </div>
  </div>
  <div class="row">
    <div class="col-12 text-center">
      <%= f.submit class: "btn btn-danger" %>
    </div>
  </div>
  </p>
  <% end %>
</div>